# Server configuration
PORT=3000

# NewsAPI.org key (optional - free tier available at https://newsapi.org/register)
# Without this key, the app uses free RSS feeds (BBC, Reuters, HackerNews)
NEWS_API_KEY=
